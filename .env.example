# Gate Environment Variables
# Gate v2 requires no environment variables for normal operation.
# These are only needed for advanced/development use cases.

# Debug mode — show stack traces on errors
# DEBUG=1

# Optional: Fortress rule signing key (for rule development)
# FORTRESS_SIGNING_KEY=your-fortress-signing-key
